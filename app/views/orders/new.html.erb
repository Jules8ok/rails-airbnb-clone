<div class="container">
	<div class="row">
		<div class="col-sm-4 col-sm-offset-4">
			<h1>New order</h1>

      <div class="container">
        <div class="row">
          <h1><%= @menu.name %></h1>
          <p> <%= @menu.address %></p>
          <p> <%= @menu.pick_up.strftime("%H:%M %m/%d/%Y") %></p>
        </div>
      </div>

			<%= render 'form', order: @order %>

		</div>
	</div>
</div>
